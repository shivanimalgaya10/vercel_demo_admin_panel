export type AuthUser = {
  id: string;
  name: string;
  email: string;
};

export type Blog = {
  _id: string;
  title: string;
  description: string;
  image: string;
  createdAt: string;
  updatedAt: string;
};

